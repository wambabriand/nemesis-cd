apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nemesis-frontend-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx" # ou autre contrôleur d'ingress utilisé
spec:
  ingressClassName: nginx
  defaultBackend:
    service:
      name: nemesis-frontend-service
      port:
        number: 80